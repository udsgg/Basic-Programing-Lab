```c++
#include <stdio.h>

void Control(int clearanceLevel, int age, int isActive);

int main() {
    int clearanceLevel, age, isActive;
    if(scanf("%d %d %d", &clearanceLevel, &age, &isActive) != 3) {
        printf("Invalid input\n");
        return 1;
    }
    if (clearanceLevel < 2 || clearanceLevel > 3) {
        printf("Access Denied\n");
        return 1;
    }
    Control(clearanceLevel, age, isActive);
    return 0;
}

void Control(int clearanceLevel, int age, int isActive) {
    if (clearanceLevel == 3 && isActive == 1) {
        printf("Access Granted\n");
        }
        else if  (clearanceLevel == 2 && isActive == 1){
        printf("Access Granted\n");
        }
        else {
        printf("Access Denied\n");
        }
    return;
}
```
