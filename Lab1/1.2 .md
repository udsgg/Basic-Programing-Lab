``` C++
#include <stdio.h>
void think(int kanan);

int main() {
    int kanan;
    if (scanf("%d", &kanan) != 1 || kanan < 0 || kanan > 100) {
    return 1; 
    }
    think(kanan);
}

void think(int kanan) {
    if (kanan >= 80) {
        printf("A\n");
    } else if (kanan >= 70) {
        printf("B\n");
    } else if (kanan >= 60) {
        printf("C\n");
    } else if (kanan >= 50) {
        printf("D\n");
    } else {
        printf("F\n");
    } 
}
```
